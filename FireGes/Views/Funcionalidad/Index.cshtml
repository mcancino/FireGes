@model IEnumerable<FireGes.Models.Funcionalidad>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionFuncionalidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Controller)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Metodo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionFuncionalidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Controller)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Metodo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdFuncionalidad">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdFuncionalidad">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdFuncionalidad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
