@model IEnumerable<FireGes.Models.CitacionVoluntario>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdCitacionNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdVoluntarioNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdCitacionNavigation.IdCitacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdVoluntarioNavigation.IdVoluntario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCitacionVoluntario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCitacionVoluntario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCitacionVoluntario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
