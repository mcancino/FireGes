@model IEnumerable<FireGes.Models.Citacion>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaCitacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoCitacionNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaCitacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoCitacionNavigation.DescripcionTipoCitacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCitacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCitacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCitacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
