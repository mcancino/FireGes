@model IEnumerable<FireGes.Models.TipoCitacion>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionTipoCitacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionTipoCitacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoCitacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoCitacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoCitacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
